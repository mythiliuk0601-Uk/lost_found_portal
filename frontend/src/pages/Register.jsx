import { useState } from 'react';
import { UserPlus, PackageSearch } from 'lucide-react';
import { api } from '../api';

export default function Register({ onRegistered, goLogin }) { const [form, setForm] = useState({ name: '', email: '', password: '', role: 'STUDENT' }); const [error, setError] = useState(''); const update = (e) => setForm({ ...form, [e.target.name]: e.target.value }); async function submit(e) { e.preventDefault(); setError(''); try { await api.register(form); onRegistered(); } catch (err) { setError(err.message); } } return <main className="login-shell"><section className="login-panel register-panel"><div className="login-card"><div className="card-logo"><PackageSearch size={40}/></div><h2>Create Account</h2><p>Join the Campus Lost &amp; Found portal</p><form onSubmit={submit}>{error && <div className="error">{error}</div>}<label>Full Name<input name="name" required placeholder="Alex Morgan" value={form.name} onChange={update}/></label><label>Email Address<input name="email" type="email" required placeholder="Enter your email address" value={form.email} onChange={update}/></label><label>Password<input name="password" type="password" minLength="6" required placeholder="At least 6 characters" value={form.password} onChange={update}/></label><label>Role<select name="role" value={form.role} onChange={update}><option value="STUDENT">Student</option><option value="STAFF">Staff</option></select></label><button className="primary full" type="submit"><UserPlus size={16}/> Create Account</button></form><p className="switch">Already registered? <button onClick={goLogin}>Sign in</button></p></div></section></main>; }
